package de.quinscape.exceed.runtime.editor.search;

public enum SearchResultType
{
    NAME,
    REFERENCE,
    TITLE,
    COMPONENT,
    ATTRIBUTE,
    STATE,
    PROPERTY,
    ROUTE,
    TRANSITION,
    ACTION,
    EXPRESSION,
    CONTEXT,
    SCOPE,
    DESCRIPTION,
    DEFAULT_VALUE, COMPONENT_ID, QUERY, RULE, QUERY_TYPE, STATE_MACHINE;
}
