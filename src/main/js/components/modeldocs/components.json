{
    "components": {
        "ModelDocs": {
            "classes": ["element"],
            "dataProvider"  : "modelDocsProvider",
            "vars" : {
                "type" : "param('type', 'de.quinscape.exceed.model.ApplicationModel')"
            }
        }
    }
}
