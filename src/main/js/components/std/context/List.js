var React = require("react");

/**
 * View context list definition component
 */
var List = React.createClass({
    render: function ()
    {
        return (<span/>);
    }
});

module.exports = List;
