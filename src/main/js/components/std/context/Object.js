var React = require("react");

/**
 * View context object definition component
 */
var Object = React.createClass({
    render: function ()
    {
        return (<span/>);
    }
});

module.exports = Object;
