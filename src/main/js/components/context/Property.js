var React = require("react");

/**
 * View context property definition component
 */
var Property = React.createClass({
    render: function ()
    {
        return (<span/>);
    }
});

module.exports = Property;
